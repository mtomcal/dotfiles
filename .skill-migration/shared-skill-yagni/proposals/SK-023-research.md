---
id: SK-023
target: research
status: verified
blocked-by: [SK-001]
source-verdict: retain substance
---

# Research: retain the durable source-backed investigation contract

## Why this item is next

SK-001 is verified and owns canonical skill-body authoring, so SK-023 is unblocked for direct normalization. WF-007 places `research` in D4 with a **retain** verdict: preserve source authority, freshness, citation, conflict, and durable-artifact gates while adding the confirmed Language Definitions and canonical body shape. This item's exact target is disjoint from concurrently claimed SK-022.

## Evidence

- `.wayfinder/shared-skill-yagni-audit/MAP.md` fixes the preserve-before-prune destination and excludes frontmatter redesign, deployment, and Pi visibility changes.
- WF-007 assigns `research` the **retain** verdict and requires its source authority, freshness, citation, conflict, and durable-artifact gates to survive D4 normalization.
- The complete WF-003 `research` record supplies the behavior ledger: route durable investigation away from routine lookup; frame the question or decision, scope, required freshness, completion criteria, primary-evidence claims, and falsifier; use an existing research-note convention or an approved/temporary location without inventing repository structure; investigate in the full authority order; preserve source metadata; distinguish primary/secondary evidence and sourced fact/inference; use read-only Herdr delegation with citation checks or a complete in-process fallback; reconcile conflicts; write all required artifact fields; and report the absolute path.
- WF-008 confirms the exact skill-local definitions of Primary source, Secondary source, Inference, Research artifact, Required freshness, and Stable citation.
- WF-006 keeps the research process with this skill while callers retain their question, tighter constraints, artifact location, gates, and return criteria. It requires universal read-only/editable isolation and full in-process fallback boundaries to remain explicit; Herdr owns command syntax, so this skill needs no copied command manual.
- `specs/ai-agent-config.md` 2.3.0 requires the canonical section order and semantic YAGNI, names `research` as the producer of durable primary-source-backed notes at an existing or approved location, preserves caller ownership under composition, permits shared checkouts only for read-only delegates, and requires a Herdr-loaded path plus in-process fallback.
- `specs/UBIQUITOUS_LANGUAGE.md` 0.7.0 leaves operational skill-local terms in the owning body and preserves the canonical body-section semantics. `specs/SPEC-OF-SPECS.md` and `specs/README.md` confirm spec authority and reading order.
- Verified `shared/skills/write-a-skill/SKILL.md` requires a behavior-preservation ledger, canonical section semantics, local guardrails/output/completion criteria, semantic YAGNI without a line target, and provenance checking. Verified `shared/skills/audit-shared-skills/SKILL.md` owns only existing union-frontmatter validation.
- The complete current `shared/skills/research/SKILL.md` is self-contained, has no linked support or executable helper, and already contains one coherent four-stage workflow. Its noncanonical numbered level-two headings and pre-section routing sentence need structural normalization, not behavioral reduction.
- Local history introduces the exact current target at `3b59c13906d5d7922ed236b19cfe548138f429d7`. `THIRD_PARTY_NOTICES.md` identifies `research` as a locally maintained adaptation of `mattpocock/skills` revision `66898f60e8c744e269f8ce06c2b2b99ce7660d5f` and reproduces the complete MIT license.
- Upstream primary-source verification on 2026-07-14 confirms that exact revision contains `skills/engineering/research/SKILL.md`, which requires primary sources, adjacent claim citations, and a single convention-following Markdown artifact; its root `LICENSE` is the same Matt Pocock MIT license reproduced in the repository notice. The local workflow is intentionally more complete and remains a one-time maintained adaptation, not an automatic mirror.
- Pi visibility is the tracked resolving symlink `pi/skills/research -> ../../shared/skills/research` and requires no edit.

No current evidence conflicts with the resolved audit. The exact target below retains the established authority order rather than replacing it with the upstream skill's shorter unranked list, and it corrects no executable syntax because the target ships no command or script.

## Exact files in scope

- `.skill-migration/shared-skill-yagni/proposals/SK-023-research.md` — item-local scope, behavior ledger, standing authorization, implementation evidence, and worker state.
- `shared/skills/research/SKILL.md` — preserve the frontmatter byte-for-byte and normalize the complete durable-investigation behavior into `Language Definitions` plus one routed `Workflow`.

No support file, script, spec, notice, test, deployment file, visibility link, migration ledger, or agent setting is authorized.

## Proposed changes

### Add

- Add the six exact WF-008 definitions under mandatory `Language Definitions`.
- Add an explicit positive routine-lookup route: answer proportionately through normal lookup and do not create a durable research artifact unless requested; continue through this Workflow only for durable investigations.
- Make each phase's observable completion local: framing records the question/decision, scope, freshness, completion criteria, primary-evidence claims, and falsifier; location selection records an existing convention or approved destination; investigation records and checks source evidence; synthesis verifies every material claim and reports the absolute path.
- State the Herdr return boundary explicitly: independent source areas may be delegated read-only, delegates do not edit the artifact or repository, and the in-process owner checks returned citations before synthesis.
- Make the direct fallback complete when Herdr is absent, unavailable, or not useful: investigate every required source area in-process, source by source, preserving citation notes before synthesis.

### Change or move

- Keep the complete frontmatter, title, trigger wording, and grants unchanged.
- Move the durable-versus-routine routing sentence into the beginning of `Workflow`, then place the existing four ordered stages under that single level-two section as level-three headings.
- Keep the exact source authority order inline: official specifications and standards; official documentation; upstream source code, tests, and release notes; first-party issue trackers or announcements.
- Keep secondary sources limited to primary-source discovery or clearly labelled interpretation. Keep sparse quoting, adjacent citations, and the sourced-fact/inference distinction beside evidence collection.
- Keep source title, stable URL or repository path, version/date, and freshness-relevant access date together as required source metadata.
- Keep artifact-location ownership with the caller: follow an existing repository convention; if none exists, ask before creating repository structure and use a user-approved path or temporary draft while waiting; never invent a repository research convention; name the artifact for the question rather than the session.
- Keep conflict reconciliation based on authority and applicable version while reporting unresolved ambiguity rather than silently choosing.
- Keep all six artifact fields inline beside writing: question and scope; cited findings; evidence table or source list with metadata; conflicts, uncertainty, and limitations; implications or recommendation; follow-up questions.
- Preserve the later-editing isolation guardrail without implying that this read-only research workflow delegates edits.

### Remove

- Remove only the four numbered level-two heading labels and the pre-section routing sentence after their behavior is retained under canonical `Workflow` routing and level-three stages.
- Add no `Activities` or `Reference` section: evidence collection and synthesis are required workflow stages, no independent recipe exists, and no support file earns conditional loading.
- Delete no trigger, route, framing field, source class, authority ordering, metadata, citation rule, delegation/fallback behavior, conflict rule, artifact field, ownership boundary, isolation rule, completion condition, provenance, license, frontmatter field, or Pi visibility behavior.

## Exact resulting production file

````markdown
---
name: research
description: Conduct durable, source-backed technical investigations using primary sources and preserve cited findings for later work. Use when a question needs sustained research, official documentation or source-code evidence, competing claims reconciled, or a reusable research artifact—not for routine factual web searches.
metadata:
  short-description: Produce durable source-backed research
allowed-tools: read,write,bash
---

# Research

## Language Definitions

- **Primary source** — direct authority such as an official specification, documentation, source, test, release note, or first-party announcement.
- **Secondary source** — interpretation or discovery aid rather than authority for the underlying claim.
- **Inference** — conclusion derived from evidence but not explicitly stated by a source.
- **Research artifact** — durable cited investigation with findings, conflicts, limitations, implications, and follow-ups.
- **Required freshness** — version/date currency needed for safe use.
- **Stable citation** — pointer specific enough to recover supporting evidence later.

## Workflow

Route the request first. For a routine factual lookup, answer through proportionate normal lookup and do not create a research artifact unless the user requests one. Continue with this workflow when the question needs a durable investigation.

### 1. Frame the question

State the decision or question, scope, required freshness, and completion criteria. Identify the claims that need primary evidence and what would falsify the current assumption.

Completion criterion: the question or decision, scope boundary, freshness requirement, completion evidence, primary-evidence claims, and falsifier are explicit.

### 2. Choose the artifact location

Follow an existing repository convention for research notes. If no convention exists, never invent one or create repository structure without approval; ask for a destination and use a user-approved path or temporary draft while waiting. Name the final Markdown artifact for the question, not the session.

Completion criterion: the final location follows an existing convention or has user approval, and any temporary draft is identified as temporary.

### 3. Investigate primary sources

Use sources in this authority order:

1. official specifications and standards;
2. official documentation;
3. upstream source code, tests, and release notes;
4. first-party issue trackers or announcements.

Use secondary sources only to discover primary material or as clearly labelled interpretation. For each source, record its title, stable URL or repository path, version/date, and access date when freshness matters. Quote sparingly, keep citations adjacent to material claims, and distinguish sourced fact from inference.

When `HERDR_ENV=1`, load the `herdr` skill and use background read-only delegation when independent source areas benefit from it. Read-only researchers may share the checkout, but they do not edit the artifact or repository. Check every returned citation against its claim before synthesis.

Outside Herdr—or when delegation is unavailable or not useful—complete the full investigation in-process, source by source, preserving citation notes before synthesis. This workflow delegates no editing; any later workflow that authorizes delegated edits must use an isolated clone or worktree.

Completion criterion: every required source area has been investigated through the Herdr path or the full in-process fallback, source metadata is captured, and delegated citations have been checked.

### 4. Reconcile and write

Reconcile conflicting sources by explaining their authority and applicable version. Report unresolved ambiguity instead of silently choosing. Write a concise Markdown artifact containing:

- question and scope;
- findings with citations adjacent to claims;
- evidence table or source list with source metadata;
- conflicts, uncertainty, and limitations;
- implications or recommendation; and
- follow-up questions.

Completion criterion: every material factual claim is traceable to a primary source or labelled as inference, citations support their adjacent claims, conflicts are reconciled or explicitly unresolved, the artifact uses the agreed location, and its absolute path is reported.
````

## Proposed skill shape

1. `Language Definitions` — present and mandatory; contains exactly the six WF-008-confirmed operational terms.
2. `Workflow` — present; one routed durable-investigation process with four ordered stages and local completion criteria.
3. `Activities` — omitted; framing, evidence collection, delegation/fallback, and synthesis are required stages rather than independently selected recipes.
4. `Reference` — omitted; the compact workflow is self-contained and has no support file.

## Behavior-preservation checklist

- [x] Frontmatter `name`, `description`, `metadata.short-description`, and `allowed-tools` remain byte-for-byte unchanged.
- [x] Durable investigation remains distinct from routine factual lookup, and routine lookup does not create an unrequested durable artifact.
- [x] Framing retains the question/decision, scope, required freshness, completion criteria, primary-evidence claims, and falsifier.
- [x] Artifact placement follows an existing repository convention or explicit user approval; absent a convention, repository structure is not invented and a temporary draft remains available while waiting.
- [x] The final artifact is named for the question rather than the session.
- [x] The complete four-level source authority order remains inline and unchanged in meaning.
- [x] Secondary sources remain discovery aids or clearly labelled interpretation rather than authority for underlying claims.
- [x] Source metadata retains title, stable URL or repository path, version/date, and freshness-relevant access date.
- [x] Sparse quotation, adjacent citation, sourced-fact/inference distinction, and citation checking remain required.
- [x] Under `HERDR_ENV=1`, the shared Herdr skill is loaded and only independent read-only source areas are delegated; delegates may share the checkout but do not edit the artifact or repository.
- [x] Outside Herdr or when delegation is unavailable/unhelpful, every source area is investigated through a complete source-by-source in-process fallback.
- [x] Any later delegated editing still requires an isolated clone or worktree.
- [x] Conflicts are reconciled by authority and applicable version, with unresolved ambiguity reported rather than hidden.
- [x] The artifact retains all fields: question/scope, cited findings, evidence/source metadata, conflicts/uncertainty/limitations, implications/recommendation, and follow-ups.
- [x] Completion requires every material fact to trace to primary evidence or be labelled as inference, citations to support adjacent claims, approved placement, and an absolute-path report.
- [x] No optional section, support file, copied Herdr command, executable helper, repository convention, ownership transfer, or fixed line target is invented.
- [x] Existing Matt Pocock source/revision/MIT coverage and Pi visibility remain unchanged and verified.

## Dependencies, provenance, and risks

- SK-001 is verified at claim baseline `c77c21cbadb9fddddd9257cdf2eb163f54a16ae8`; no unfinished owner interface blocks this retain-substance normalization.
- The target has no linked support and no executable source or command syntax. Herdr command mechanics remain with the `herdr` skill; this item preserves only the routing, read-only authority, citation-return, and full-fallback contract.
- The existing target says to prefer Herdr delegation, while the normative spec says a delegating skill SHOULD load Herdr. The exact result preserves both by loading Herdr whenever the `HERDR_ENV=1` branch is taken and selecting read-only delegation only when useful.
- The current phrase “use a user-specified path or temporary draft while waiting” could be read as allowing an invented repository path. The exact result clarifies the already-audited boundary: no new repository convention or structure without approval; a temporary draft remains the safe fallback.
- `Fact` is not added as a new defined term. The confirmed `Inference` definition and explicit sourced-fact/inference distinction supply the required evidence boundary without inventing language beyond WF-008.
- `THIRD_PARTY_NOTICES.md` already records the imported source, full revision, local-fork posture, and complete MIT license. Upstream exact-revision source and license checks agree with it, so notice editing would be unjustified scope growth.
- Risk: “routine factual lookup” could be mistaken for permission to answer without evidence. Mitigation: route it to proportionate normal lookup, not to unsupported recall, while reserving the durable artifact contract for sustained research.
- Risk: stronger completion criteria could be mistaken for a new artifact schema. Mitigation: they only make the existing framing, evidence, citation, conflict, and path gates observable; the six existing artifact fields remain unchanged in substance.
- Risk: Herdr delegation could accidentally transfer writer ownership. Mitigation: delegates are explicitly read-only, return evidence for citation checking, and never edit the artifact or repository.

## Verification

1. Reread the complete resulting `shared/skills/research/SKILL.md` and compare it exactly with the proposal's resulting production file.
2. Compare the result against the complete WF-003 ledger, exact WF-008 definitions, WF-006 composition boundaries, and every behavior-preservation checklist item above.
3. Parse headings and assert level-two headings are exactly `Language Definitions` then `Workflow`; assert `Activities` and `Reference` are absent.
4. Compare the opening YAML block byte-for-byte with baseline `c77c21cbadb9fddddd9257cdf2eb163f54a16ae8`; parse it with PyYAML and confirm all four union fields, a sub-1024-character description containing `Use when`, and unchanged `read,write,bash` grants.
5. Run a full existing-union-schema audit over every discovered `shared/skills/*/SKILL.md` with a YAML-aware parser and perform the semantic least-tool check required by the verified audit skill. Acceptance: all catalog entries are accounted for and the target has no finding; unrelated findings are reported but not edited.
6. Run `find shared/skills/research -mindepth 1 -maxdepth 2 -print` and a Markdown-link scan. Acceptance: only `SKILL.md` exists and no undisclosed support, script, asset, or unresolved relative link exists.
7. Run `test -L pi/skills/research && test "$(readlink pi/skills/research)" = '../../shared/skills/research' && test -f pi/skills/research/SKILL.md`. Acceptance: Pi visibility remains the unchanged resolving symlink.
8. Inspect `THIRD_PARTY_NOTICES.md`, exact-revision upstream source/license evidence, and local Git history. Acceptance: `research`, source revision `66898f60e8c744e269f8ce06c2b2b99ce7660d5f`, local-fork posture, full MIT text, and local introduction commit remain accounted for without a notice edit.
9. Run `git diff --check` and `bash tests/run.sh`.
10. Inspect `git diff --name-status c77c21cbadb9fddddd9257cdf2eb163f54a16ae8 --` and scoped diffs. Acceptance: only this proposal and `shared/skills/research/SKILL.md` differ; `MIGRATION.md`, `pi/settings.json`, specs, notices, visibility links, other skills, and unrelated proposals remain unchanged.
11. Commit the two authorized files, verify the worktree is clean, and report branch, commit, baseline/range, files, checks, concise diff, and residual risks without claiming coordinator verification.

Observable acceptance criteria:

- The resulting production file exactly matches proposal revision 1.
- Its only level-two sections are `Language Definitions` then `Workflow`.
- Every WF-003 behavior remains inline with exact confirmed definitions and no ownership transfer.
- Frontmatter, provenance/MIT coverage, Pi visibility, and protected paths remain unchanged.
- The complete catalog passes the existing union-frontmatter audit, repository tests pass, and no executable/support verification is falsely claimed.

## Implementation and verification record

Focused verification completed: `2026-07-14T18:04:12+00:00`.

- Actual scope: exactly this proposal and `shared/skills/research/SKILL.md`; production diff is 38 insertions and 25 removals. No support file exists or changed.
- Exact proposal comparison: PASS. The complete target equals proposal revision 1's exact resulting production file.
- Behavior and language: PASS. The complete WF-003 ledger, six exact WF-008 definitions, WF-006 ownership boundaries, durable/routine route, question/falsifier/freshness framing, approved location, authority order, source classes and metadata, fact/inference distinction, read-only Herdr path, complete fallback, citation checks, conflict handling, artifact fields, and absolute-path report are retained inline.
- Canonical shape: PASS. Level-two headings are exactly `Language Definitions` then `Workflow`; no unearned `Activities`, `Reference`, link, support file, script, or asset exists.
- Frontmatter: PASS and byte-identical to claim baseline. PyYAML confirms all union fields, 314-character `Use when` description, and unchanged `read,write,bash` grants. The body still uses `read` for sources, `write` for the artifact, and `bash` for repository/source inspection; no grant is newly unused.
- Complete union-frontmatter audit: PASS. PyYAML parsed all 33 deterministically discovered skills with 0 errors and 0 description warnings. The other 32 bodies/frontmatters are unchanged from the clean claim baseline; focused semantic review confirms the only changed body's grants remain necessary, so the least-tool result does not change.
- Provenance/history: PASS. Exact-revision upstream source retains primary-source, cited Markdown, and existing-convention behavior. The upstream MIT legal text exactly matches the Matt Pocock notice section, which records `research`, revision `66898f60e8c744e269f8ce06c2b2b99ce7660d5f`, and local-fork posture. Local introduction remains `3b59c13906d5d7922ed236b19cfe548138f429d7`.
- Pi visibility: PASS. The tracked symlink remains `../../shared/skills/research` and resolves to the changed canonical skill.
- Protected paths: PASS. `MIGRATION.md`, `pi/settings.json`, specs, notices, AGENTS, tests, deployment, visibility links, other skills, and unrelated proposals are unchanged. Recorded `THIRD_PARTY_NOTICES.md` and `pi/settings.json` SHA-256 values remain `2db33befec4673cff931e358bd6c9896f8e6c4b36a2d5a1b7b0b682a3282e38e` and `ccd1befa8017b51901c026ad005256b037c7341847cf93c95525d7ec31e52c03`.
- `git diff --check`: PASS. `bash tests/run.sh`: PASS (2 shell files, 12 tests).
- Resulting target SHA-256: `1ec0da0d8b8629338d136545ed6cf36c8549f671af35a4c1ded0182b6afa6c5f`.
- Residual risk: “proportionate normal lookup” intentionally leaves routine-answer depth context-sensitive. It prevents unrequested durable artifacts without weakening the durable branch's primary-source gates. No executable support or live Herdr command exists in this scope to validate.

Coordinator integration verification completed at `2026-07-14T18:08:53+00:00` against integrated commit `f0f5abf`: the complete target and proposal were reread; six definitions, durable/routine routing, framing/falsifier/freshness, approved location, authority order, source classes/metadata, adjacent citations, fact/inference distinction, read-only Herdr path, complete in-process fallback, citation checking, conflict handling, artifact fields, absolute-path output, frontmatter identity, Pi visibility, Matt Pocock/MIT provenance, and exact scope passed independent checks. One initial focused assertion expected the phrase “source title” while the target correctly says “its title”; the corrected semantic assertion passed and no production change was needed. The YAML-aware audit accounted for all 33 skills with zero errors and warnings, `git diff --check` passed, and `bash tests/run.sh` passed all 12 tests. The protected `pi/settings.json` content and diff hashes remained `7d3f4713d7239e8cda3f75597a55f1767fecdf7964324630e4e74dc686d12e05` and `8298a9063e3ebf2988000d176057a41c31cf20c10bbabe92fb57d47f9f2738d5`. Residual risk remains the intentionally context-sensitive depth of routine lookup.

## Explicit exclusions

- No edits to `.skill-migration/shared-skill-yagni/MIGRATION.md`, `.wayfinder/`, specs, `THIRD_PARTY_NOTICES.md`, `AGENTS.md`, tests, scripts, install/deployment files, Pi visibility symlinks, `pi/settings.json`, unrelated skills, or unrelated proposals.
- No support Markdown, Activity, Reference, script, asset, repository research directory, universal artifact schema, copied Herdr command, live Herdr id, or new skill.
- No frontmatter redesign, new field, grant portability change, discovery/deployment change, or automatic upstream synchronization.
- No weakening of primary-source authority, no deletion of routine routing or complete fallback, and no fixed line-count target.
- No claim that this worker performs coordinator integration or verification.

## Standing authorization

- Human directive timestamp: `2026-07-14T15:55:39+00:00`
- Proposal revision: `1`
- Scope check: `PASS — MAP → WF-007 → complete WF-003 research record → WF-008 → WF-006 → current specs → verified write-a-skill/audit-shared-skills → complete target and directly linked support (none) → provenance/history → exact-revision upstream source/license were read in authority order; exact two-file scope, complete behavior ledger, retain-substance verdict, confirmed definitions, routing, artifact ownership, evidence/citation/conflict contracts, read-only delegation/full fallback, frontmatter, provenance/MIT, Pi visibility, exclusions, contradiction review, and observable verification criteria pass. Production editing may continue autonomously under standing directive 2026-07-14T15:55:39+00:00 for proposal revision 1.`
