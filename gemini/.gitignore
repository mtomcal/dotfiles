# Gemini CLI - exclude sensitive and local runtime data

# Authentication and credentials
mcp-oauth-tokens.json
.credentials.json

# Per-machine / runtime state
trustedFolders.json
mcp-server-enablement.json

# Session, history, and cache data
tmp/
history/
cli-browser-profile/
sessions/

# Logs
*.log
